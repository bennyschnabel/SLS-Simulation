function [s] = computateLayerThickness(L, n)
    % [s] = computateLayerThickness(L, n)
    %
    % Bachelor thesis equation number: (3.8)
    % 
    % Calculation of the layer thickness.
    % The layer thickness has the unit m.
    %
    
    s = L / n;
end